#include <fstream>
#include <vector>
#include <set>
#include <unordered_map>

std::vector<std::set<int>> adjMatrix;  

void createAdjs(int n, )  { // n, adjLists
  // adjMatrix = {}
  for (int i = 0; i < n; i++)  {
    std::set<int> iAdjs; 
    for (int j = 0; j < n; j++)  {
      if ()
    }
  }
  // for i in range(n): 
  //   iAdjs = set([])
  //   for j in range(n): 
  //     if adjLists[i][j] == '1': 
  //       iAdjs.add(j)
  //   adjMatrix[i] = iAdjs
}

int main()   {
  // read from a file 
  std::ifstream file("gp41.txt");
    std::string str; 
    while (std::getline(file, str))
    {
        // Process strg
    }
  
  // adjMatrix = 
  return 0; 
}